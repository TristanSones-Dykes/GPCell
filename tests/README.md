## Testing directory for the library

Contains unit tests to deal with both functionality and performance checks. The tests are written using the `unittest` framework and can be run using the `unittest` module.